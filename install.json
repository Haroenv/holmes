{
  "resources": {
    "body": [{
      "type": "script",
      "src": "js/holmes.js"
    }, {
      "type": "script",
      "contents": "var h = new holmes(INSTALL_OPTIONS)"
    }]
  },
  "options": {
    "properties": {
      "input": {
        "title": "input",
        "description": "A querySelector to find the input",
        "type": "string",
        "format": "selector",
        "default": "input[type=search]"
      },
      "find": {
        "type": "string",
        "format": "selector",
        "description": "A querySelectorAll rule to find each of the find terms"
      },
      "placeholder": {
        "type": "string",
        "format": "element",
        "description": "text to show when no results"
      },
      "class": {
        "visible": {
          "type": "string",
          "format": "selector",
          "description": "class to add to matched elements"
        },
        "hidden": {
          "type": "string",
          "format": "selector",
          "default": "hidden",
          "description": "class to add to non-matched elements"
        }
      },
      "dynamic": {
        "type": "boolean",
        "default": false,
        "description": "if true, this will refresh the content every search"
      },
      "mark": {
        "type": "boolean",
        "default": false,
        "description": "highlight the matching text"
      },
      "minCharacters": {
        "type": "number",
        "default": 0,
        "description": "minimum amount of characters needed for Holmes to start matching"
      },
      "hiddenAttr": {
        "type": "boolean",
        "default": false,
        "description": "use [hidden] as well to hide non-matching elements without css changes"
      }
    }
  }
}
